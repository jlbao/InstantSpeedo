<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin" tools:context=".MainActivity"
    android:id="@+id/activity_main">

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Become A Host"
        android:id="@+id/becomeHostButton"
        android:layout_above="@+id/findNearByDevicesButton"
        android:layout_alignLeft="@+id/findNearByDevicesButton"
        android:layout_alignStart="@+id/findNearByDevicesButton"
        android:layout_alignRight="@+id/findNearByDevicesButton"
        android:layout_alignEnd="@+id/findNearByDevicesButton" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Find Nearby Devices"
        android:id="@+id/findNearByDevicesButton"
        android:layout_centerVertical="true"
        android:layout_centerHorizontal="true" />
</RelativeLayout>
